'use client';

import { VisualFlowBuilder } from '@/components/qr/visual-flow-builder';
import type { AdminComponent } from '.';

const DataMatrixFlowBuilder: AdminComponent = () => {
  return <VisualFlowBuilder />;
};

export default DataMatrixFlowBuilder;
