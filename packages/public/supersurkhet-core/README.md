# @supersurkhet/core

Framework-agnostic schema helpers and typed CRUD hooks used by Supersurkhet.

- No split between "core" and "feature" schemas.
- Consumers own `schema.ts` and keep full control.
- Works with optional `@supersurkhet/zod-typegen-vite` for `db.d.ts` generation.
