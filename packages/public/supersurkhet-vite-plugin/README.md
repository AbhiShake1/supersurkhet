# @supersurkhet/zod-typegen-vite

Optional Vite plugin that reads your `schema.ts` default export and emits a `db.d.ts` global type.

```ts
import { zodTypegen } from '@supersurkhet/zod-typegen-vite';
```
